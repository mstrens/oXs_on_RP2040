/*
#pragma once
#include <stdint.h>

#define BUFFER_COMM_MAX 30
class COMM_WITH_RX
{
public:
    COMM_WITH_RX() ;
    void begin();
    bool void send(uint8_t data);
    bool void send(uint8_t * data, uint8_t length)
private:
    uint8_t bufferCount;
    uint8_t buffer(BUFFER_COMM_MAX);
}
*/